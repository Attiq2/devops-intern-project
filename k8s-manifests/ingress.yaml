apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-intern-ingress
spec:
  rules:
    - host: devops-intern.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-intern-service
                port:
                  number: 80

